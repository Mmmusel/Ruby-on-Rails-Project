<%= form_with(model: cart) do |form| %>
  <% if cart.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cart.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
        <% cart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label "数量", style: "display: block" %>
    <%= form.number_field :num %>
  </div>

</br>
  <div>
    <%= form.submit "提交修改" , class: "linkpro",id: "linkpro"%>
  </div>
<% end %>
